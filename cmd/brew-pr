#!/bin/sh

set -euo pipefail

version=$(brew livecheck -q "$1" --json | jq -r '.[] | select(.version.outdated==true) | .version.latest')
if [ -z "$version" ]; then
    echo "WARNING: $1 is up to date"
    exit 0
fi

echo "Bumping $1 to $version"
brew bump-formula-pr "$@" --version="$version"
